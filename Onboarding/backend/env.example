# Backend environment variables
PORT=4000
CORS_ORIGIN=http://localhost:5175
SUPABASE_URL=
# Prefer SERVICE_ROLE for server-side inserts if you need RLS bypass in local dev
SUPABASE_SERVICE_ROLE_KEY=
# Or you can use anon key if RLS configured for service access
SUPABASE_ANON_KEY=
